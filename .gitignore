# Hugo build output and transient lock file
/public/
/.hugo_build.lock

# Hugo's generated image cache
/resources/_gen/

# Local tooling — never commit (also matches the node_modules symlink)
node_modules
